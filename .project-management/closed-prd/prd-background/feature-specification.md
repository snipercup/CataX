Create `Assets/Fonts/` and relocate the three `.ttf` files (`Apple ][.ttf`, `Roboto-Bold.ttf`, `Glitch.ttf`) and their `.import` files.\
Update all resource paths referencing these fonts (e.g., `scene_selector.tscn`).
